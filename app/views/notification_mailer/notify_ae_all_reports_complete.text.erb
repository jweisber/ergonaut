Dear <%= @recipients_list %>,

All the reports commissioned for "<%= @submission.title %>" are now complete. Please enter a decision within <%= JournalSettings.days_after_reports_completed_to_submit_decision %> days.

<% if @submission.area_editor %>
You can access the submission here: <%= one_click_edit_url(@submission.auth_token) %>

<% end %>
<%= render 'email_footer' %>