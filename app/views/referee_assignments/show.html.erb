<div class="row">
  <div class="offset2 span8">
		
		<h3 class="center">
			<%= link_to "\"#{@submission.title}\"",
									@submission.manuscript_file.url %>
		</h3>
		<% if current_user.managing_editor? %>
			<h4 class="center">
				by <%= @submission.author.full_name %>
			</h4>
		<% end %>

		<%= render 'shared/completed_report' %>
		
  </div>
</div>
